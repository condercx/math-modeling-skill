# A-F 选题与 Q1..QN 拆解

三人各负责两题初筛，每题记录题意、子问题依赖、数据/附件、候选模型、实现/算力/验证风险、资料可得性、四天内最大风险与最小交付路径。主 Agent 负责筛选证据；可按项目路由在明确任务边界内让子代理并行扩展来源和候选，但不独立拍板。

主 Agent 对六题做数学结构、数据/实现、论文/截止风险三视角复筛；可由独立只读质检 Agent 发现遗漏。最终主选题/备选题由人类会议决定并记录 DECISIONS。

选题后每问记目标、输入、输出、约束、指标、上游依赖、下游消费者、Human Owner 和状态，画 DAG；不要默认全串行或全独立。
