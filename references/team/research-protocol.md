# 调研与来源协议

由主 Agent 负责收敛证据与候选；需要并行宽搜、文献梳理、候选对照或失败模式排查时，按项目模型路由和用户授权选择子代理。使用当前环境可用、合规的检索工具，**不强制 AnySearch**。不得把真实赛题或队内数据发到公开网页/队外人员。

每个 qN 视需要覆盖：领域机理/经典模型、近年改进、数据指标与验证、公开实现、局限和失败模式。把查询记录、来源与候选分别存入 `research/qN/`；注明 URL/题名、结论、适用条件、所需数据、局限、可信度与是否纳入主线 shortlist。关键模型/公式须可追溯并由人核验；子代理候选不能直接作为最终依据。
