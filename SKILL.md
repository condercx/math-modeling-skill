---
name: math-modeling
description: 当用户要求数学建模、建模竞赛、建模分析、代码求解、结果可视化或生成数学建模论文时使用。支持建模、编程、论文三个独立阶段，项目规则优先，质量检查按需进行。
---

# 数学建模

本 Skill 提供建模、编程、论文和研究工具。生成内容必须由参赛队员理解、核验和改写。

## 项目规则优先

- 开始前读取项目 `AGENTS.md`。项目规则与 Skill 冲突时，以项目规则为准。
- 一次性比赛项目默认采用轻量模式：不要求 `STATUS/TASKS/DECISIONS`、固定 Handoff、预算审批、固定 owner、模型路由或逐轮审核。
- `M1/P1/P2/W1/W2` 只可作为可选检查标签。没有使用这些标签不表示任务未完成。
- 除非项目规则要求，否则直接开工、运行、迭代和记录结果，不为每次修改创建审批文件。

## 硬约束

1. 原始题目、官方代码、官方 config、原始输入和官方附件只读；需要运行官方代码时，把输出写到新的实验或结果目录。
2. 最终论文主稿、`submission/locked/`、已锁定 PDF 和 MD5 事实不得被普通开发 Agent 覆盖。论文草稿、分节材料、模型、代码和结果可以继续迭代。
3. AI 使用标注、最终人工核验、官方模板、匿名要求和最终 PDF MD5 按当届官方规则执行。
4. 不编造数值、引用或运行结果；失败和未知项如实保留。

## 路由

| 任务 | 读取 |
|---|---|
| 完整建模 | `references/roles/建模手/SKILL.md` -> `references/roles/编程手/SKILL.md` -> `references/roles/论文手/SKILL.md` |
| 只做分析或选模型 | `references/roles/建模手/SKILL.md` |
| 只实现、运行或画图 | `references/roles/编程手/SKILL.md` |
| 只写或修改论文 | `references/roles/论文手/SKILL.md` |
| 需要算法建议 | `references/算法索引.md`，再读相关 `assets/*.md` |
| 使用 PDF/Excel/DOCX/LaTeX/科研图/论文检索 | 对应 `tools/*/SKILL.md` |
| 真实竞赛或截止时间 | `references/交付与截止时间协议.md` |

单阶段任务不强制跑完整三角色流程。

## 阶段产物

### 建模

- `题目分析报告.md`
- `术语表格.md`

模型必须覆盖题目约束和评价目标，公式、假设、参数、验证方式和来源可追溯。每个子问题通常不超过两个独立模型体系；同一物理机制的不同精度展开不重复计数。

### 编程

- Python 或 MATLAB 求解代码
- 真实运行结果表
- 必要的结果图、过程图和流程图
- 复现说明或清单

先跑通最小真实路径，再扩大实验。图表必须来自真实数据；数值和单位要检查。不会因为尚未通过某个固定门禁而禁止继续。

### 论文

- 默认 Word；用户明确要求时增加 LaTeX/PDF
- 当届官方模板决定版式、匿名、篇幅、附件和命名
- 正文只使用可追溯结果，引用要打开原始来源核验
- 内部检查记录不得进入正式论文

## 按需质检

复杂模型、高风险代码、正式结果或最终论文可以由未参与编写的 Agent 做只读复核。检查标签可用 `M1/P1/P2/W1/W2` 命名，也可直接用“模型复核、代码复核、结果复核、论文复核”。

需要时读取 [references/Subagent调度.md](references/Subagent调度.md)。不需要质检不影响继续工作；发现问题时由原执行者修正并记录证据即可。

## 2026 华为杯

目标为 2026“华为杯”时，读取 `references/competitions/huawei-cup-2026.md`，并优先满足当届官方规则：

- 仓库与材料保持私有。
- 使用官方附件 3 模板，保留封面和 4 个 logo，摘要外匿名。
- AI 辅助分析、代码和模型按附件 4 标注，最终文字人工改写。
- 最终 PDF 命名、上传和 MD5 锁定按官方要求执行；锁定后不得覆盖或重导出。

需要准备官方附件时，可运行：

```bash
python "<SKILL_ROOT>/scripts/huawei2026_prepare.py" "<PROJECT_ROOT>" --source-dir "<官方附件目录>"
```
