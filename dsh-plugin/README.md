# LEGACY / ARCHIVE ONLY

本目录是旧 DeepSeek Harness 自包含预设，包含历史 `mm_*` 工具、五门禁 UI 和状态文件逻辑。它不再用于新项目，也不是当前协作入口。

新项目请直接使用本仓库根目录的 `SKILL.md`、`TEAM-SKILL.md` 和项目 `AGENTS.md`。若项目采用一次性轻量模式，不使用本目录中的状态机、Gate、预算或 Handoff 规则。

保留本目录只为复现旧部署和追溯历史。不要复制、安装或修改其中的 JS 逻辑来承载新的团队流程。
